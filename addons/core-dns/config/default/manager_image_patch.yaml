apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: controller
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: justinsb/coredns-controller:latest
        name: manager
